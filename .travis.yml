language: cpp
compiler: gcc
sudo: true
install:
  - sudo apt-get install libgtest-dev -y
  - cd /usr/src/gtest
  - sudo mkdir build
  - cd build
  - sudo cmake ..
  - sudo make
  - sudo make install
  - cd ../
script:
  - cmake CMakeLists.txt -DMATH_BUILD_TESTS=ON
  - make
  - ctest
